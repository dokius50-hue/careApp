# Netlify config for Vite + React PWA
[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
